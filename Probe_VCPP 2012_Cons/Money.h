#include <iostream>

struct money{
	int hrn;
	short int kop;
}
	void add(money::add(money other));
	// Додавання
	void multiply(money.multiply(int quantity));
	// Множення
	void round();
	// Округлення
	void print() const;
	// Вивід у консоль
;{}
